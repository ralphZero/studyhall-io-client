import React from 'react';
import HeaderTitle from './HeaderTitle';

const PlanHeader = () => {
  return (
    <div className='flex items-center px-6 border-0 border-b border-dividerDark border-solid h-14 w-full bg-[#ffffff]'>
      <HeaderTitle />
    </div>
  );
};

export default PlanHeader;
